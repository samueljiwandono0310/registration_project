import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class WigetMaterial {
  Widget registrationStepNumber({@required Colors color}) {
    return Center(
      child: Container(
        height: 200,
        width: 200,
        color: Colors.white,
      ),
    );
  }
}
